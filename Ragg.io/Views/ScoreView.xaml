<UserControl x:Class="Ragg.io.Views.ScoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Border VerticalAlignment="Center" Width="120" Background="{Binding BackgroundColor}">
        <TextBlock FontWeight="{Binding FontWeight}" Foreground="{Binding ForegroundColor}" FontSize="20"  TextAlignment="Center" Text="{Binding ScoreLabel}"></TextBlock>
    </Border>
</UserControl>
